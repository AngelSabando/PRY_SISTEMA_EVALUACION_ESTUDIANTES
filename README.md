# PRY_SISTEMA_EVALUACION_ESTUDIANTES
Para lograr ejecutar correctamente este proyecto se necesita que Bootstrap@5.3.8, inicialmente se lo descargo con Node.js ademas se utilizo Vite para lograr levantar el local
Usaremos los siguientes comandos:
npm init -y
npm install vite
npm install bootstrap@5.3.8

Luego modificaremos el archivo "package.json" en la seccion de scripts añadiremos:
"start": "vite",
"build": "vite build"

para poder correr el servidor vite con:
npm run start

y luego escribir en consola la letra "o" que abre en si el servidor